import React from 'react';
import ListContainer from './ListContainer';
import { shallow } from 'enzyme';


describe('Search', () => {

    let wrapper;

    beforeEach(() => {
        wrapper = shallow(
            <ListContainer />
        );
    });

});
